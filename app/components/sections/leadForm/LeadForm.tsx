import React from 'react'
import s from "./leadform.module.css"

const LeadForm = () => {
  return (
    <div className={`${s.leadFormCont}`}>
        LeadForm
    </div>
  )
}

export default LeadForm